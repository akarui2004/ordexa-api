{
  "watch": ["src", "server.ts", "config", ".env.local", ".env"],
  "ext": "ts,json,toml",
  "ignore": ["node_modules", "dist"],
  "exec": "ts-node -r tsconfig-paths/register server.ts",
  "legacyWatch": true
}
